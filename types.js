//Here will see the types of Typescript.
//to initialze a variable as string
var j = "new string";
//To initialize variable as number
var k = 5;
//To initialize variable as array of numbers
var p = [1, 2, 3];
//To initialize a dynamic variable; value can be redeclared
var d = 8;
d = "changed value as string";
//To initialize variable as boolean
var say = true;
{
    console.log(j, k, p, d, say);
    console.log(say);
}
